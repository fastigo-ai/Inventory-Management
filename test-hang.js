const axios = require('axios');

async function run() {
  try {
    console.log("Logging in...");
    // Assuming there is an admin login. Wait, I don't know the password.
    // Let's just create a test token by importing the JWT function!
  } catch(e) {
    console.log(e);
  }
}
run();
